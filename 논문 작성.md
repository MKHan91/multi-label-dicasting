논문 작성

1. 논문의 핵심 가설 (Research Hypothesis)
다이캐스팅 결함은 형상 (structure)과 질감 (Texture) 이 혼재 되어 있는데, 이를 AutoEncoder만의 정상 데이터 만을 이용해서 anomaly detection 을 하겠다. 

P: hole이 존재. --> 형상학적으로 결함이다. --> Reconstruction Error 관점에서 설명할 수 있다. 
S: 얇은 (실 같은)미세한 결함 표면에 존재. --> 질감의 차이가 존재.--> Gram matrix를 이용하여 Style을 캐치한다.

2. 논문의 방법론(Research Methodology)
* 1 단계: Multi-scale Feature Fusion (Encoder - Decoder)
- ResNet의 인코더 각 층(layer) S1, S2, S3을 Skip connection으로 디코더에 연결을 함.
    그 이유는 낮은 층 (low-level feature)은 미세한 결함을 담당하고, 
    높은 층 (High-level feature)은 전체적인 구조를 담당하기 때문이다. 

* 2 단계: Texture Alignment via Cos-Gram
- 일반적으로는 Gram matrix를 유클리디언 거리를 이용해서 구함. 
- 하지만, 유클리디언 거리은 단순히 거리를 나타내는 개념이기 때문에 
    고차원 상에서 거리가 짧다고 해서 서로 유사한 것은 아님. 
    즉, 코사인 유사도를 이용해서 결함을 찾아낸다. 
    코사인 유사도를 사용하는 또 다른 이유는 유클리디언 거리는 밝기 차이에 굉장히 민감함. 코사인 유사도는 패턴의 방향성에만 집중을 하기 떄문에 밝기 변화에 robust.

* 3 단계: Statistical Threshold (Kernel Density Estimation, KDE 기반한 임계치 설정)
- 정상 (학습 데이터)를 가우시안 KDE로 모델링하여, 상위 5% 지점을 임계치로 설정. 

3. 실험 설계 (Experiments)
- 방법론들 존재.
    - Base AE (L1 만 사용.)만 했을 때 성능을 측정.
    - SSIM AE 만 했을 때 성능 측정.
    - 제안한 방법 (Recon + Gram 기반 코사인 유사도) 만 했을 때 성능 측정. 

- Ablation study 
    * recon_score, gram_score가 각각 얼마의 비율로 존재해야 최고의 성능이 나오는지 모두 실험. (0.5도 포함.)

    * KDE 그래프: 정상 데이터로부터 나온 밀도 함수가 어떻게 정상과 결함을 판단하는 기준을 제공하는지 설명.

4. 수식
- Hybrid anomaly score (A)
    A = P * recon loss + Q * (1 - Consine sim(Gram matrix))
- Gram matrix normalization (0~1 사이로 맞춰줌)
    Gram matrix = F * F^T (HW x C) (C x HW)

5. 결론 (Conclustion) 및 향후 과제 (Future work)
- 본 연구는 다이캐스팅의 특유의 질감 변화를 감지하기 위해 Gram matrix 기반한 코사인 유사도를 도입했으며, 소량의 정상 데이터만으로도 고성능의 이상탐지를 가능하게 했다. 

- 본 연구는 전체 이미지를 통계치에 기반하여 결함을 탐지하지만 결함 부위를 구체적으로 시각화하는 anomlay localization (heatmap) 연구로 확장한 것이다. 
- 본 연구를 psuedo label로서 multi-label classifcation에 적용할 것이다. 